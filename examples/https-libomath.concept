// Normal HTTPS using the operating system certificate store.

import std::https;

fn main() -> int {
    std::https request;
    request.host = "libomath.org";
    request.path = "/";

    if (!request.get()) {
        println(request.error);
        return 1;
    }

    print(request.response);
    return 42;
}
